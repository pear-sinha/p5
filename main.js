function preload(){
}

function setup(){
    canvas = createCanvas(400, 400);
    canvas.center();
}

function draw(){
    background("yellow");
   // text(mouseX + "," + mouseY, mouseX, mouseY);
   // circle()
}